{
  "name": "overwatch",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "demo:watch": "concurrently -n Core,Demo \"yarn workspace @overwatch/core watch\" \"yarn workspace demo start\""
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "prettier": "^2.8.1",
    "process": "^0.11.10"
  }
}
